import joi from "joi";
import connection from "../dbStrategy/postgres.js";

export async function postRental(req, res, next) {
  try {
    
  } catch (error) {
    res.sendStatus(500);
    console.error(error);
  }
}